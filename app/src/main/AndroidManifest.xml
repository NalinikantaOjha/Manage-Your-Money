<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.masai.naliniapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManageYourMoney">
        <activity
            android:name=".ui.activity.EditActivityActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.TestActivity2"
            android:exported="true" />
        <activity
            android:name=".ui.activity.AddActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.AddTypeActivity"
            android:exported="true"
            android:label="@string/title_activity_add_type"
            android:theme="@style/Theme.ManageYourMoney.NoActionBar" />
        <activity
            android:name=".ui.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>